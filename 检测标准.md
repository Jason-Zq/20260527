# 文件留底检测标准

> 本文档记录**文件留底检测 / 业务审核**当前使用的判定标准，供业务方 / PSD 复核对照。
> **权威来源是代码**：单文件判定见 `backend/llm_service.py:_build_archive_detect_prompt`，批次总判见 `_build_judge_batch_overall_prompt`，分类体系见 `ARCHIVE_CATEGORIES_FULL`，前端默认 criteria 见 `frontend/src/components/ArchiveDetectEntryPage.vue:buildBizCriteria`。**改 prompt 必须同步更新本文档**，否则会漂移。改后端 prompt 还需重启 worker 才生效。

---

## 判定核心口径（PSD 业务标准，适用于所有进展）

**留底的本质是证明「该进展对应的服务确已启动或发生」，不是必须有官方回执类关键件。**

- 证据可以是**官方文件**（批复函 / 受理回执 / 递交确认）。
- 也可以是**软证据的组合**：服务合同（写明具体服务内容，完成合同中任意一项即视为服务启动）+ 聊天记录 / 邮件 / 确认截图（客户确认凭证，如客户确认转卖房产 = 该项服务已启动）。
- 合同 + 聊天记录 / 确认截图**不是无关附件**，组合起来即构成证明服务已启动的关键证据链。

三档结论：

| 结论 | 条件 |
|------|------|
| **match（符合）** | 存在能证明该进展服务已启动 / 已发生的证据（官方件 **或** 软证据组合，任一即可） |
| **partial（部分符合）** | 只有辅助性附件，既无官方关键件、也无任何能证明服务已启动的软证据组合（材料不齐） |
| **mismatch（不符合）** | 完全没有任何文件能体现该进展的服务已启动或发生 |

---

## 单文件判定规则

单文件判定产出 `verdict`（match / partial / mismatch / no_text）+ `match_score`（0-100）。判定指南：

1. **关联人扩展**：同一客户的文件集合中，配偶 / 子女 / 父母 / 共同申请人的文件也视为相关，不要因文件人名与客户姓名不一致就判 mismatch。
2. **关联人姓名 / 拼音匹配**：文件中出现的姓名若匹配任一关联人（含拼音或英文转写，如「关晓晓」=「GUAN Xiaoxiao」、「邓士凯」=「Deng Shikai」），即视为强关联，判 match / partial。授权委托书（Power of Attorney）、海牙认证（Apostille）等文件委托人 / 当事人常为家属或办理人，属有效留底；**官方回执、递交确认、受理通知等文件上的「申请人」姓名通常对应办理人而非客户本人，若文件上姓名与客户不一致但与办理人（含拼音/英文转写）一致，视为本进展有效留底，不判 mismatch。**
3. **OCR 容错**：标准证件格式（身份证 / 护照 / 房产证）即使 OCR 乱码，也判 match / partial，不因 OCR 噪声判 mismatch。
4. **软证据有效**：服务合同（写明服务内容）、客户确认类聊天 / 邮件 / 截图是有效留底证据，能证明服务已启动。合同归 C 类，确认 / 放弃类沟通归对应分类（如 I 类或备注为服务启动凭证），**不因非标准证件格式判 mismatch**。
5. **服务启动标注**：若文件体现服务已启动 / 客户确认某项服务内容，在 `key_points` 中明确标注（如「体现服务已启动」「客户确认转卖房产」），供总体判定参考。
6. **转款凭证**：以金额 + 币种为核心证据，与项目投资标准吻合即判 match，**不强制体现客户姓名**（如泰币 650000 = 泰国精英签青铜卡投资金额）。
7. **银行开户类项目**（美国 / 新加坡个户等）：账户核心材料属客户隐私，银行常在开户后直接通过邮件联系客户，文案侧未必持有核心原件。识别到客户账户信息 / 开户信息表 / 账户开通 / 转款等任一凭证即判 match。银行通用材料（隐私通知、网点列表、汇款模板、防诈骗指南等）属正常附带件，不因其内容通用、未直接体现客户信息就判 mismatch。
8. **阶段错配不硬否决**：文件更适用于其他阶段（如递交前的身份证 / 资产证明出现在递交后批次），只要文件本身可归入分类体系且与客户 / 项目相关，最多判 partial，不判 mismatch；银行开户等隐私类项目对阶段匹配不作强制要求。
9. **无文字文件**：OCR / 抽取后无有效文字的文件，verdict 标 `no_text`、status=done（不算失败、不重试），**不参与总体判定**。

---

## 批次总体判定规则

所有文件完成后，把全部文件明细 + criteria + stage 喂给 LLM 综合判定 overall。判定原则：

- **0（核心）**：见上文「判定核心口径」。
- **1**：一个批次通常包含**关键证据**（能证明该进展 / 服务成立，含官方件或软证据组合）和**附带文件**（身份证、护照等辅助材料）。
- **2**：**只要存在能证明该进展服务已启动 / 已发生的证据（官方件或软证据组合任一即可），整批即判 match**，附带文件的低分不应拉低整体结论。
- **3**：只有辅助性附件、既无官方关键件、也无任何能证明服务已启动的软证据组合 -> partial（材料不齐）。
- **4**：完全没有任何文件能体现该进展的服务已启动或发生 -> mismatch。
- **5（转款凭证）**：金额 + 币种与项目投资标准吻合即为有效留底，不要求体现客户姓名。
- **6（银行开户类项目）**：账户核心材料属隐私、文案侧常无原件，只要有客户账户信息 / 开户信息表 / 开通 / 转款等任一凭证即判 match；银行通用材料（隐私通知、网点列表、汇款模板、防诈骗指南）属正常附带件，不据此降级。
- **7（阶段错配不硬否决）**：文件更适用于其他阶段（如递交前材料出现在递交后批次）不影响整体符合性，银行开户等隐私类项目对阶段匹配不作强制要求。
- **8（单份失败不拖垮整批）**：单份文件检测失败 / 加密无法读取，**只影响该文件，不影响其他文件的整体判断**，不据此判整批 mismatch。

**回退兜底**：LLM 综合判定调用失败时，回退规则平均分（avg≥80 -> match / ≥50 -> partial / <50 -> mismatch）+ `summarize_batch` 文本兜底。全批无可判定文件（全 no_text 或全 error）时，overall 标为「检测未成功」而非「内容不符」。

---

## 前端默认判定提示词（criteria 模板）

业务审核 tab 进入时由 `buildBizCriteria` 自动预填，用户手改后不再自动覆盖。这是拼进单文件 prompt 的「用户判定标准」：

```
请按公司文件留底标准，审核此文件是否为 {客户代号/客户名 / 项目 / 项目详情 / 进展} 在「{递交前|递交后}」阶段应上传的留底文件。重点判断文件类型、内容完整性和格式规范，而不是严格匹配文件上的姓名（该客户的文件可能属于其配偶/子女/父母）。留底的核心是证明该进展对应的服务已启动或发生：只要合同中约定的任意一项服务内容有对应凭证即视为服务启动，证据可以是官方文件，也可以是服务合同 + 聊天记录/邮件/确认截图等软证据组合（如客户确认转卖房产即视为该项服务已启动），软证据同样有效。

关联人关键词：{客户姓名、办理人}。文件内容中若出现上述任一姓名（含其拼音或英文转写，如「关晓晓」对应「GUAN Xiaoxiao」），即视为与本进展强关联，可支持判定为符合，不要因文件当事人姓名与客户不一致而判为不符合--授权委托书/公证认证等文件的委托人可能是客户的家属或本进展的办理人；官方回执、递交确认、受理通知等文件上的「申请人」姓名通常对应本进展办理人而非客户本人，若文件上姓名与客户不一致但与办理人（含拼音/英文转写）一致，视为本进展有效留底，不判 mismatch。
```

要点：

- 客户标签用 `client_code` 而非姓名，不作姓名硬匹配，仅为上下文。
- 关联人关键词段落仅在有客户姓名或办理人时才追加；二者都为空则不带这段。
- 用户手改 criteria 后标 dirty，业务字段变化不再覆盖；可点「重置为自动生成」恢复。

---

## 公司售后留底分类体系

业务审核传 `stage=pre_submit|post_submit`，使用完整分类树 + 阶段提醒；客户姓名与办理人由后端从 DB 显式注入 prompt。

### 递交前阶段应上传的 5 大类

- **A. 客户基础文件**：护照、身份证、中文信息表、个人简历、出生证明类文件、户口本、毕业证书 & 学位证书、结婚证 & 离婚证等婚姻状态文件、港澳通行证、房产证、工作证明信等
- **B. 客户个人文件**：职业 & 专业证书、照片、社保 / 个税、不反对移民申明、无犯罪记录证明、体检类文件、地址证明、在读证明、资产 / 资信文件、公证 / 认证文件、成就类文件、学生签证、成绩单、疫苗本、翻译类文件、证书或获奖记录、客户访校指南、单身证明、个人文件等
- **C. 客户公司文件**：营业执照、章程、股东会决议、验资报告、公司财报 / 审计报告、银行流水、业务合同 / 合作协议、组织架构图、股东名册、雇佣合同、公司介绍、办公室照片、办公室租赁合同等
- **D. 其他备用文件**：律师文件、投资文件、批复留底文件、使馆申请表、合规部 KYC 留底文件、开户 KYC 文件、投资证明、购房文件、入境处申请表格、劳工卡申请、正签信、工作签证留底、商业计划书等
- **E. 转款凭证**：服务过程中涉及的转账凭证等

### 递交后阶段应上传的 4 大类

- **F. 文案制作的递交文件类**：递交全套留底、递交后补料留底等
- **G. 获批 / 失败**：筛选 / 名额通知函、使馆信、入境处信件、批复函、录取通知、补料信、打款通知、体检通知、获身份文件、拒签信、撤案信等任何客户相关批复类文件
- **H. 其他文件**：客户获批后协助客户留存的重要证件信息，如更新护照、入境小白条等
- **I. 停滞 / 放弃类文件**：客户明确表示撤案、不再继续办理、不启动了 / 放弃办理了等主观原因不再继续办理项目的邮件、聊天等截图

---

## 输出字段

### 单文件判定输出

| 字段 | 说明 |
|------|------|
| `verdict` | match / partial / mismatch / no_text |
| `match_score` | 0-100 整数 |
| `doc_category` | 字母编号 + 子类名，如 `A-护照`、`G-批复函`；无法精确到子类则填字母 + 大类名如 `A-客户基础文件` |
| `reason` | 30-120 字判断依据，仅引用分类标准 / 用户判定相关内容 |
| `key_points` | 3-6 条要点 bullets |

> `reason` / `key_points` 要求脱敏：金额 / 电话 / 身份证号 / 银行卡号 / 账号等敏感信息用 `[金额]` / `[手机号]` / `[身份证]` / `[银行卡]` 占位。OCR 原文不入库，入库的是脱敏后的 `archive_detect_files.ocr_text`。`is_archival=(verdict=='match')`、`confidence=match_score` 用于向后兼容。

### 批次总体判定输出

| 字段 | 说明 |
|------|------|
| `overall_verdict` | match / partial / mismatch |
| `overall_score` | 0-100，与 verdict 一致：match ≥ 80、partial 50-79、mismatch < 50 |
| `overall_reason` | 80-200 字总体说明，含整体结论依据 / 关键证据命中 / 缺失或问题点，脱敏 |

---

## 维护说明

- 改 `backend/llm_service.py` 的 prompt 后，**必须重启 worker**（`doc-review-worker@1`）才生效；uvicorn 的 `--reload` 不会重启 worker。
- 改前端 `buildBizCriteria` 默认模板后，需重新构建前端 dist 并部署。
- 改任一判定口径后，**同步更新本文档**，避免与代码漂移。
- 历史批次可用后台「按新规则批量重判」(`admin/rejudge-overall`，只重跑总判、每批 1 次 LLM) 或「批量重审」(`admin/rerun-files-batch`，重跑每个单文件、复用 ocr_text) 回归验证新口径。
