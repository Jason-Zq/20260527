# 文件留底检测 - 后端依赖
# 与主项目同版本，便于直接复用主项目 .venv312 虚拟环境

# Web 框架
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.29

# LLM 客户端
openai==2.38.0

# URL 下载 (file_fetcher.py)
httpx==0.28.1

# OCR 与 PDF
paddleocr==2.10.0
paddlepaddle==2.6.2
pdfplumber==0.11.9
pypdfium2==5.8.0
pillow==12.1.0
numpy==2.3.5

# DOCX 抽取
python-docx==1.2.0

# paddlepaddle 运行时依赖（不显式列会被漏掉）
setuptools>=70.0.0
